# Peakda 백엔드 요청 사항 — 검색 노출(SEO)·공유 카드

> **작성일**: 2026-09-15 · **기준 스펙**: PEAKDA API v1 (Develop: `https://api-dev.peakda.com`)
>
> 웹을 검색엔진(구글·네이버)과 SNS 공유에 노출하려고 작업 중입니다. 스팟 상세·피드 상세는 비로그인 조회가 가능해 프론트에서 끝냈고, **아래는 백엔드 조치가 있어야 진행할 수 있는 것만** 모았습니다.
>
> 검색엔진·SNS 크롤러는 **쿠키 없이** 페이지를 가져갑니다. 그래서 "로그인해야 보이는 API"와 "시간이 지나면 만료되는 이미지 URL"은 검색 결과·공유 카드에 쓸 수 없습니다.

---

## 요약

| # | 우선순위 | 대상 | 요청 |
|---|---|---|---|
| 1 | **P0** | `GET /api/festivals/{id}`, `GET /api/curations/{id}`, `GET /api/curations` | 비로그인 조회 허용 |
| 2 | P1 | 기록 사진·동네 스팟 대표 사진 (+ 에디토리얼 이미지 확인) | 만료 없는 공개 이미지 URL |
| 3 | P2 | sitemap 용 목록 | 공개 스팟·축제·큐레이션 id + 수정일 목록 |
| 4 | P2 | 개화 추정 | 데이터 출처·판정 기준 설명 |

---

## P0

### 1. 축제·큐레이션 조회 API 비로그인 허용

현재 스펙에서 아래 3개만 `accessTokenCookie` 인증이 걸려 있습니다. 같은 성격의 조회 API(`GET /api/spots/{id}`, `GET /api/feed/{id}`, `GET /api/explore/festivals` 등)는 이미 비로그인 조회가 됩니다.

```
GET /api/festivals/{id}     인증 필요 → 비로그인 허용
GET /api/curations/{id}     인증 필요 → 비로그인 허용
GET /api/curations          인증 필요 → 비로그인 허용
```

**지금 생기는 문제**

- 축제·큐레이션 상세는 검색 가치가 가장 높은 페이지인데, 크롤러가 받는 화면에 **축제명·기간·장소가 없습니다.** 공유 카드도 기본 이미지로만 나갑니다.
- 비로그인 사용자는 탐색 화면에서 축제·큐레이션 카드를 눌러도 **로그인 시트만 뜹니다.** 목록(`/api/explore/festivals`)은 보이는데 상세는 못 보는 상태입니다.

**확인 부탁드릴 것**

- `FestivalDetailResponse`, `CurationDetailResponse`, `CurationCardResponse` 에는 사용자별 필드가 없어 **비로그인 응답이 로그인 응답과 같다**고 이해했습니다. 다르게 내려야 하는 값이 있으면 알려주세요.
- 발행 전(초안) 큐레이션·에디토리얼이 비로그인 응답에 섞이지 않는지 확인 부탁드립니다.

**반영되면 프론트에서 할 일**: 두 페이지 모두 서버 렌더링으로 전환하고, 축제에 `Event` 구조화 데이터를 붙이고, sitemap 에 URL을 추가합니다.

---

## P1

### 2. 만료 없는 공개 이미지 URL

공유 카드(`og:image`)와 검색 구조화 데이터의 이미지는 크롤러가 **며칠·몇 주 뒤에 다시 가져갑니다.** 지금 사진 URL은 presigned URL이라 그때 깨지므로 **기본 카드로 대체**하고 있습니다.

| 대상 | 현재 | 프론트 처리 |
|---|---|---|
| `SpotRecordResponse.photos[].url`, `SpotRecordSummaryResponse.coverPhoto` / `photos[].url` | presigned (만료 있음) | 피드 상세 공유 카드에 사진을 못 쓰고 기본 카드 사용 |
| `SpotDetailResponse.representativeImageUrl` (LOCAL 스팟) | 최근 방문 기록 사진 (만료 있음) | 동네 스팟은 공유 카드·구조화 데이터에서 이미지 제외 |

**요청**: 위 이미지에 대해 **만료 없는 공개 URL**(CloudFront 등 CDN 경로)을 주실 수 있을까요?

```jsonc
// PhotoEntry (기존 url 은 그대로 두셔도 됩니다)
{
  "objectKey": "...",
  "url": "https://...presigned...",
  "publicUrl": "https://cdn.peakda.com/records/..."   // nullable, 만료 없음
}
```

- 필드 이름이나 방식(기존 `url` 을 CDN 주소로 바꾸기 등)은 편하신 쪽으로 정해주세요.
- 원본 대신 **공유 카드용 리사이즈본(가로 1200px 정도)** 하나만 공개해도 충분합니다.

**함께 확인 부탁드릴 것**

- `FestivalEditorialResponse.heroImageUrl`, `CurationDetailResponse.heroImageUrl`, `ExploreFestivalItem.thumbnailUrl` 도 업로드 API가 `objectKey` 로 저장하는 구조라 **만료되는 URL인지** 궁금합니다. 만료된다면 1번이 반영돼도 축제·큐레이션 공유 카드에 쓸 수 없으니 같은 방식으로 부탁드립니다.
- 공개 URL이 생기면 **삭제·비공개 처리된 기록이나 차단된 사용자의 사진**도 주소만 알면 계속 열립니다. 이 부분이 정책상 괜찮은지 판단 부탁드립니다.

---

## P2

### 3. sitemap 용 공개 페이지 목록

`/sitemap.xml` 에 스팟 상세 주소를 넣으려면 전체 스팟 id가 필요한데, 목록 API가 없어서 **개화 지도(`GET /api/seasonal/blooms`)를 한반도 전체 범위로 한 번 조회**해 모으고 있습니다.

**지금 방식의 한계**

- 지도 핀으로 안 나오는 스팟은 sitemap 에서 빠집니다.
- 스팟마다 수정일이 없어서, 모든 URL에 개화 기준일(`baseDate`) 하나를 똑같이 넣고 있습니다.
- 축제·큐레이션은 1번이 반영돼도 id를 모을 목록이 마땅치 않습니다. `/api/explore/festivals` 는 진행 중인 축제만 내려줍니다.

**요청** (형태는 자유):

```jsonc
// 예: GET /api/sitemap
{
  "spots":     [{ "id": 166, "updatedAt": "2026-09-14T00:00:00+09:00" }],
  "festivals": [{ "id": 12,  "updatedAt": "..." }],
  "curations": [{ "id": 3,   "updatedAt": "..." }]
}
```

- 비로그인 조회, **공개 상태인 것만** 포함해 주세요.
- 프론트는 하루 한 번만 호출합니다.
- 급하지 않습니다. 없어도 지금 방식으로 동작합니다.

### 4. 개화 추정 데이터 출처·판정 기준

검색엔진(특히 AI 요약 검색)은 **"어디서 온 정보인지"가 드러난 페이지**를 더 신뢰합니다. 스팟 상세에 "M.D 기준 개화 예측이에요"는 이미 표시하고 있고, 출처와 기준 한 줄을 더 붙이려고 합니다. API 변경 요청이 아니라 **문구에 쓸 사실 확인**입니다.

1. 개화 추정의 **원천 데이터**가 무엇인가요? (기상청 관측·예보, 산림청, 자체 모델, 방문 기록 반영 여부 등)
2. `PREPARING / STARTED / PEAK / ENDED` 는 **어떤 기준으로 판정**하나요? (예: 만개 추정일 ±N일)
3. `BloomBanner.confidence` 는 무엇을 뜻하고, 사용자에게 보여줘도 되는 값인가요?
4. 화면에 **"○○ 데이터를 바탕으로 한 추정"** 식으로 출처를 적어도 문제없나요? 라이선스·표기 의무가 있으면 알려주세요.

---



## 이번에 요청하지 않는 것

- `GET /api/users/{id}` 도 인증이 필요해 사용자 프로필(`/users/{id}`)은 비로그인으로 볼 수 없습니다. 프로필을 검색에 노출할지는 개인정보 측면에서 기획 판단이 먼저라 **이번 요청에서는 뺐습니다.**
